.build/
DerivedData/
*.xcuserstate
xcuserdata/
.DS_Store

# PRD
Notiee 产品需求文档 (PRD) v1.0.md

# Ignoring tmn folder
tmn/
.gemini/
.workbuddy/

# mem0 upstream snapshot (local reference only)
mem0-main/

# Android port docs — local staging, will be moved to the Android repo
docs/android/

# Xcode Organizer archive export output (.ipa + export logs)
*.ipa
Notiee+ 20*/

# Backend (Tencent SCF) — source is versioned; dependencies and deploy bundles are local
notiee-ping-stream/node_modules/
notiee-ping-stream/.DS_Store
notiee-ping-stream*.zip

# Xcode build output (local artifact, 28M+)
build/
