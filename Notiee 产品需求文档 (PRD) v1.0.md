# **Notiee 产品需求文档 (PRD) v1.0**

## **1\. 文档概述**

* **产品名称：**Notiee  
* **版本号：**v1.0 (MVP)  
* **目标平台：**iOS (原生 Swift / SwiftUI)  
* **撰写时间：**2026年5月21日  
* **产品定位：**一款基于日程感知的 AI 极速随手记与知识提炼工具，通过“日程对应”自动赋予碎片化照片以上下文，利用“Vision \+ Text 大模型”实现课堂与会议记录的非结构化数据自动结构化。

## **2\. 产品愿景与核心价值**

* **核心痛点：**学生及职场人在课堂或会议中频繁拍摄 PPT、黑板或白板，课后这些照片散落在系统相册中，难以检索、懒得整理，知识点无法沉淀。  
* **解决方案：**Notiee 通过导入系统日历或 .ics 课表文件，在用户按下快门的瞬间自动关联当前时间段的课程或会议；同时通过后台异步队列调用用户自带的 AI 大模型 API，自动进行图片 OCR、摘要生成、小标题提炼和待办事项提取，让碎片化的“拍照”转化为结构化的“知识流”。

## **3\. 功能设计与交互细则 (Functional Design)**

Notiee 采用底栏 3+1 药丸悬浮式导航结构（Today、拍记、记录、我），整体设计语言偏向克制、高效、暗色调适配。

### **3.1 模块一：📅 Today (今日控制中心)**

* **上半部分：时间轴与课表感知 (固定区域)**  
  * **日程数据源：**支持通过 .ics 文件导入或直接读取 iOS 原生系统日历。  
  * **状态展示：**纵向展示今日日程，状态分为“已完成”、“正在进行”（当前时间命中的课程卡片高亮显示）和“即将到来”。  
  * **边缘场景处理：**若当前时间段无任何日程，则显示“无事件”，并在用户拍照时自动提示归入“未分类”分组。  
* **下半部分：今日动态展板 (BottomSheet 可上滑展开抽屉)**  
  * **待办事项 (To-do List)：**展示由 AI 自动提取的行动项，支持用户手动勾选标记完成。  
  * **今日记录 (Today's Records)：**按时间由新到旧排列展示今天拍下的所有照片卡片。

### **3.2 模块二：📷 拍记 (内置极速相机)**

* **界面布局：**全屏沉浸式取景框，暗色调 UI。底部中央为大圆形快门键，左下角为“暂存区/未分类”入口及缩略图预览，右下角为系统相册导入。  
* **情景感知叠加层 (Contextual Overlay)：**取景框顶部中央半透明悬浮显示当前环境感知。  
* **无缝连拍反馈：**界面不出现任何阻断式的加载动画、遮罩或确认弹窗。照片通过平滑的飞入动画缩小落入左下角暂存区，取景框始终保持活跃。

### **3.3 模块三：📓 记录 (知识库管理)**

* **传统精准搜索：**支持检索文件夹名称、AI 生成的小标题、AI 摘要、以及底层 OCR 识别出的原始图片文字。  
* **文件夹区域：**包含系统自动文件夹、暂存区/收件箱、自建文件夹。  
* **课程二级详情页：**左侧纵向时间轴线，右侧按“周次 \+ 具体日期”由新到旧排列。

### **3.4 模块四：👤 我 (设置与自助 AI)**

* **首屏偏好设置：**提供单选列表自定义冷启动 App 时默认进入的页面。  
* **AI API 自助接入（核心二级菜单）：**文本处理大模型、图像识别大模型、测试连接。  
* **安全与隐私声明：**API Key 加密存储在手机本地设备中。

## **4\. 技术架构与系统设计 (Technical Architecture)**

Notiee 采用前端离线优先、后端异步削峰的混合架构。

### **4.1 前端技术栈 (iOS Native)**

* **UI 框架与样式：全面采用 Apple 原生设计语言 (Human Interface Guidelines)。代码层面优先使用 SwiftUI 提供的系统级原生组件（如原生 Navigation、BottomSheet、系统图标 SF Symbols、原生的毛玻璃 Blur 效果），尽量减少非必要的自定义样式，以保证应用在 iOS 生态中的沉浸感、运行流畅度以及系统级的一致性。**  
* **硬件交互：**AVFoundation（捕获原生相机数据流，自定义快门逻辑）。  
* **本地数据存储：**SwiftData (或 Core Data)，支持无网环境下的秒开和本地关键词检索。  
* **设计模式：**MVVM (Model-View-ViewModel)。

### **4.2 后端技术栈 (Backend Cloud)**

* **API 框架：**Python (FastAPI)。  
* **任务队列中间件：**Redis。  
* **数据库：**PostgreSQL，原生支持 JSON/JSONB。

## **5\. 数据库表结构设计 (Database Schema)**

以下为后端 PostgreSQL 与前端 SwiftData 映射的核心表结构设计：

### **5.1 课程日程表 (courses)**

| 字段名 | 类型 | 约束 | 描述 |
| :---- | :---- | :---- | :---- |
| id | UUID | PRIMARY KEY | 课程唯一 ID |
| user\_id | VARCHAR(64) | NOT NULL | 用户唯一识别码 |
| course\_name | VARCHAR(128) | NOT NULL | 课程/会议名称 |
| start\_time | TIMESTAMP | NOT NULL | 日程开始时间 |

### **5.2 笔记记录表 (records)**

| 字段名 | 类型 | 约束 | 描述 |
| :---- | :---- | :---- | :---- |
| id | UUID | PRIMARY KEY | 记录唯一 ID |
| course\_id | UUID | FOREIGN KEY | 关联课程ID |
| raw\_image\_url | VARCHAR(512) | NOT NULL | OSS 原始图片访问链接 |
| ai\_summary | TEXT | NULL | 大模型提炼的知识点摘要 |

### **5.3 待办事项表 (todos)**

| 字段名 | 类型 | 约束 | 描述 |
| :---- | :---- | :---- | :---- |
| id | UUID | PRIMARY KEY | 待办唯一 ID |
| content | VARCHAR(256) | NOT NULL | 待办具体内容 |
| is\_completed | BOOLEAN | DEFAULT FALSE | 是否完成 |

## **6\. AI 大模型集成策略与 Prompt 设计**

* **阶段一：图像大模型 (Vision LLM) 任务**: 提取 OCR（包含 LaTeX 公式）并生成 100 字内核心摘要。  
* **阶段二：文本处理大模型 (Text LLM) 任务**: 提炼 12 字精准小标题，并判断提取行动项生成 Todos。

## **7\. 异常处理与边缘场景**

* **AI API 频次受限：**指数退避重试机制，若 3 次失败移入死信队列，前端支持手动重试。  
* **课表冲突与重叠：**关联最近更新或创建时间最晚的日程，允许用户手动切换。  
* **完全无网络环境：**直接缓存于手机本地，网络恢复后自动在后台重传。