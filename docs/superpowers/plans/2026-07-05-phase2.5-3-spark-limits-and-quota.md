# Phase 2.5 + 3: Spark 档位限制 & 额度展示 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 免费版 Spark 按档位收紧（仅 flash、单会话 20 轮、记忆 5 条、无 Agent），并在「我」tab 展示后端额度（本月 X/N 篇进度环 + Pro 入口），回顾页按篇数重做。

**Architecture:** 新增**唯一一处**档位分叉出口 `SparkTierLimits`（`#if NOTIEE_PLUS` 恒不限；免费版读硬编码的 `CurrentEntitlement.tier`）。Spark 的三处强制点（发送轮数、记忆写入、Agent 开关）只读这个中心出口。额度展示新增一个**只读**的 `EntitlementStore`（拉 `/me/quota` + 监听 `.backendQuotaUpdated` 广播），驱动 `QuotaCard` 与 `UsageReviewView`；`MeView` 用 `#if` 分叉插卡片、切回顾页。

**Tech Stack:** Swift / SwiftUI / Combine / XCTest；双 target（`Notiee` 免费 / `Notiee+` BYOK）靠 `NOTIEE_PLUS` 编译标志区分。

**已核对的现状（Phase 0/1/2 已落）：**
- `CuratedModelCatalog` / `CuratedModelSelection` / `CurrentEntitlement.tier`（硬编码 `.free`）已存在（`Notiee/Models/CuratedModelCatalog.swift`）。
- `BackendAPIClient` 有 `getJSON(path:)`（自动带 Bearer）；`BackendQuota(tier/used/limit/remaining)` + `.backendQuotaUpdated` 广播（userInfo `["quota": BackendQuota]`）+ `broadcast()` 已存在。
- `AuthService.shared.bearerToken: String?` 已存在（无 `isLoggedIn`，用 `bearerToken != nil` 判断）。
- Spark：`SparkViewModel.roundCount = messages.count/2`、`newConversation()`（:421）、记忆写入循环（:243-246）、`isAgentModeEnabled`；Agent 开关 UI 在 `SparkView.swift:158` 的 `SparkAgentChip`，旁边有个 BYOK `SparkModelChip`（依赖 `availableModels`）。
- `MeView`：账户 Section（:23-73）、回顾入口 `ReviewView(store:)`（:97-104）。`NoteRecord`：`capturedAt: Date` / `eventID: UUID?` / `title: String` / `isDeleted: Bool`。

**隔离规则（全程遵守）：**
- 新文件 `SparkTierLimits.swift`（含 `#if`）、`EntitlementStore.swift`、`QuotaCard.swift`、`UsageReviewView.swift`、测试文件 → **只勾 `Notiee` target**（除 `SparkTierLimits` 有 `#if NOTIEE_PLUS` 分支、仍只勾 Notiee 亦可，见 Task 1 说明）。
- 共享文件（`SparkViewModel` / `SparkView` / `SparkAgentChip` / `AgentSettingsView` / `MeView`）**只读中心出口 `SparkTierLimits`**，或用 `#if NOTIEE_PLUS`，**不写运行时 `if 免费版`**。
- 档位真值 Phase 4 才有；本 Phase 一律用 `CurrentEntitlement.tier`（硬编码 `.free`）。`EntitlementStore` 仅用于**展示**，不参与 gating。

---

## Testing 说明（每个「运行测试」步骤通用）

本机若 `xcodebuild` 报 `requires Xcode`：`sudo xcode-select -s /Applications/Xcode.app`。命令：

```bash
xcodebuild test -project Notiee.xcodeproj -scheme Notiee \
  -destination 'platform=iOS Simulator,name=iPhone 16' \
  -only-testing:NotieeTests/SparkTierLimitsTests \
  -only-testing:NotieeTests/EntitlementStoreTests
```

模拟器名用 `xcrun simctl list devices available | grep iPhone` 挑真实存在的替换；或 Xcode 里 `⌘U` / 点测试类菱形。测试跑在 `Notiee`（免费）target，故 `NOTIEE_PLUS` 未定义、走免费分支。

---

# Part A — Phase 2.5：Spark 档位限制

## Task 1: 中心限额出口 SparkTierLimits（TDD）

**Files:**
- Create: `Notiee/Features/Spark/SparkTierLimits.swift`（只勾 Notiee target）
- Test: `NotieeTests/SparkTierLimitsTests.swift`（只勾 Notiee 测试）

> 说明：此文件带 `#if NOTIEE_PLUS` 分支，两个 target 语义不同，但**只勾 Notiee target 即可**——因为它只被免费版独有的强制逻辑和共享文件的 `SparkTierLimits.xxx` 调用。若 Notiee+ 也需要它（本 Phase 不需要），再加入即可。当前保持 [N]。

- [ ] **Step 1: 写失败的测试**

创建 `NotieeTests/SparkTierLimitsTests.swift`：

```swift
import XCTest
@testable import Notiee

final class SparkTierLimitsTests: XCTestCase {

    // 测试跑在 Notiee(免费) target，CurrentEntitlement.tier == .free

    func testFreeTierSessionRoundCapIs20() {
        XCTAssertEqual(SparkTierLimits.maxSessionRounds, 20)
    }

    func testFreeTierMemoryCapIs5() {
        XCTAssertEqual(SparkTierLimits.maxMemoryEntries, 5)
    }

    func testFreeTierAgentNotAllowed() {
        XCTAssertFalse(SparkTierLimits.isAgentAllowed)
    }

    func testFreeTierAllowsOnlyFlashText() {
        XCTAssertTrue(SparkTierLimits.isTextModelAllowed("deepseek-v4-flash"))
        XCTAssertFalse(SparkTierLimits.isTextModelAllowed("MiniMax-M3"))
        XCTAssertFalse(SparkTierLimits.isTextModelAllowed("does-not-exist"))
    }

    func testSessionRoundLimitBoundary() {
        XCTAssertFalse(SparkTierLimits.sessionRoundLimitReached(currentRounds: 19))
        XCTAssertTrue(SparkTierLimits.sessionRoundLimitReached(currentRounds: 20))
        XCTAssertTrue(SparkTierLimits.sessionRoundLimitReached(currentRounds: 21))
    }

    func testAcceptedNewMemoryCountClampsToCap() {
        // 已 5 条、还想写 3 条 → 0
        XCTAssertEqual(SparkTierLimits.acceptedNewMemoryCount(existingCount: 5, incoming: 3), 0)
        // 已 3 条、还想写 3 条 → 只收 2（3+2=5 封顶）
        XCTAssertEqual(SparkTierLimits.acceptedNewMemoryCount(existingCount: 3, incoming: 3), 2)
        // 已 0 条、写 4 条 → 全收
        XCTAssertEqual(SparkTierLimits.acceptedNewMemoryCount(existingCount: 0, incoming: 4), 4)
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

`⌘U`（或命令行）。Expected: 编译失败 —— `SparkTierLimits` 未定义。

- [ ] **Step 3: 写最小实现**

创建 `Notiee/Features/Spark/SparkTierLimits.swift`（**只勾 Notiee target**）：

```swift
import Foundation

/// 中心化的 Spark 档位限额出口。**全 App 唯一按档位分叉之处**：
/// Notiee+（BYOK）恒为「不限」；Notiee（免费）读 `CurrentEntitlement.tier`
/// （Phase 4 前恒 `.free`）。所有 Spark 限额调用方只读这里，不各自判断。
enum SparkTierLimits {

    // MARK: - 档位能力

    #if NOTIEE_PLUS
    static var maxSessionRounds: Int? { nil }        // nil = 不限
    static var maxMemoryEntries: Int? { nil }
    static var isAgentAllowed: Bool { true }
    static func isTextModelAllowed(_ id: String) -> Bool { true }
    #else
    private static var isPro: Bool { CurrentEntitlement.tier == .pro }
    static var maxSessionRounds: Int? { isPro ? nil : 20 }
    static var maxMemoryEntries: Int? { isPro ? nil : 5 }
    static var isAgentAllowed: Bool { isPro }
    static func isTextModelAllowed(_ id: String) -> Bool {
        guard let model = CuratedModelCatalog.model(id: id), model.kind == .text else { return false }
        return CuratedModelCatalog.isAllowed(model, for: CurrentEntitlement.tier)
    }
    #endif

    // MARK: - 纯函数（供强制点调用 + 单测）

    static func sessionRoundLimitReached(currentRounds: Int) -> Bool {
        guard let max = maxSessionRounds else { return false }
        return currentRounds >= max
    }

    /// 给定已存记忆条数与本次待写入新条数，返回**允许写入的新条数**。
    static func acceptedNewMemoryCount(existingCount: Int, incoming: Int) -> Int {
        guard let cap = maxMemoryEntries else { return incoming }
        return Swift.max(0, Swift.min(incoming, cap - existingCount))
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

`⌘U`。Expected: `SparkTierLimitsTests` 全 PASS。

- [ ] **Step 5: 提交**

```bash
git add Notiee/Features/Spark/SparkTierLimits.swift NotieeTests/SparkTierLimitsTests.swift
git commit -m "feat(spark): central SparkTierLimits tier gating exit (free: 20 rounds / 5 memory / no agent / flash only)"
```

---

## Task 2: 在 SparkViewModel 强制轮数上限 + 记忆上限

**Files:**
- Modify: `Notiee/Features/Spark/SparkViewModel.swift`（新增 `sessionLimitReached`；`sendMessage` 头部；记忆写入循环 :243-246；`newConversation` :421 复位）

- [ ] **Step 1: 新增已达上限的发布属性**

在 `SparkViewModel` 属性区，`@Published var memoryActionText: String?` 那行下面加：

```swift
    /// 免费档单会话轮数触顶（Notiee+ 恒 false）。View 据此显示「开启新会话 / 升级 Pro」横幅。
    @Published var sessionLimitReached: Bool = false
```

- [ ] **Step 2: sendMessage 头部拦截超限**

在 `func sendMessage()` 里，把这段：

```swift
        if SparkAIService.containsInjectionPattern(t) {
            injectionWarning = String(localized: "输入包含不安全的指令，请修改后重试")
            return
        }
        injectionWarning = nil

        inputText = ""; state = .loading
```

改为（在清空输入之前加轮数拦截）：

```swift
        if SparkAIService.containsInjectionPattern(t) {
            injectionWarning = String(localized: "输入包含不安全的指令，请修改后重试")
            return
        }
        injectionWarning = nil

        if SparkTierLimits.sessionRoundLimitReached(currentRounds: roundCount) {
            sessionLimitReached = true
            return
        }

        inputText = ""; state = .loading
```

- [ ] **Step 3: 记忆写入循环套上限**

把记忆写入那段（`// Memory operations` 到三个 for 循环结束）：

```swift
            // Memory operations
            let ms = SparkMemoryStore.live
            var memoryCount = 0
            for (k, v) in ops.toSet { ms.set(k, value: v); memoryCount += 1 }
            for (k, v) in ops.toUpdate { ms.set(k, value: v); memoryCount += 1 }
            for k in ops.toDelete { ms.delete(k); memoryCount += 1 }
```

替换为：

```swift
            // Memory operations（免费档记忆上限：只对「新键」计数，更新/删除不受限）
            let ms = SparkMemoryStore.live
            let existing = (try? ms.load()) ?? [:]
            let newKeys = ops.toSet.keys.filter { existing[$0] == nil }
            let allowedNewCount = SparkTierLimits.acceptedNewMemoryCount(
                existingCount: existing.count, incoming: newKeys.count)
            let acceptedNewKeys = Set(newKeys.prefix(allowedNewCount))
            var memoryCount = 0
            for (k, v) in ops.toSet {
                if existing[k] == nil, !acceptedNewKeys.contains(k) { continue } // 超上限的新记忆丢弃
                ms.set(k, value: v); memoryCount += 1
            }
            for (k, v) in ops.toUpdate { ms.set(k, value: v); memoryCount += 1 }
            for k in ops.toDelete { ms.delete(k); memoryCount += 1 }
```

- [ ] **Step 4: 新会话复位标志**

在 `func newConversation()` 内，任意一行执行语句后加（确保开新会话后横幅消失）：

```swift
        sessionLimitReached = false
```

- [ ] **Step 5: 编译确认通过**

选 `Notiee` scheme，`⌘B`。Expected: BUILD SUCCEEDED。

- [ ] **Step 6: 运行 Task 1 测试确认未回归**

`⌘U`（只跑 `SparkTierLimitsTests`）。Expected: 全 PASS（Notiee+ 里 `maxSessionRounds`/`maxMemoryEntries` 为 nil，逻辑退化为不限，行为不变）。

- [ ] **Step 7: 提交**

```bash
git add Notiee/Features/Spark/SparkViewModel.swift
git commit -m "feat(spark): enforce free-tier session round cap and memory cap via SparkTierLimits"
```

---

## Task 3: Agent 加锁 + 隐藏 BYOK 模型芯片 + 会话上限横幅

**Files:**
- Modify: `Notiee/Features/Spark/SparkAgentChip.swift`（加 `locked` 能力）
- Modify: `Notiee/Features/Spark/SparkView.swift`（Agent 芯片传 locked；`#if` 隐藏 BYOK 模型芯片；加会话上限横幅 + Pro 提示 alert）
- Modify: `Notiee/Features/Settings/AgentSettingsView.swift`（免费档顶部加锁提示）

- [ ] **Step 1: SparkAgentChip 支持锁定态**

把 `Notiee/Features/Spark/SparkAgentChip.swift` 的 `struct SparkAgentChip` 整体替换为：

```swift
struct SparkAgentChip: View {
    @Binding var isOn: Bool
    var locked: Bool = false
    var onLockedTap: () -> Void = {}

    var body: some View {
        Button {
            if locked {
                onLockedTap()
            } else {
                withAnimation(.easeInOut(duration: 0.15)) { isOn.toggle() }
            }
        } label: {
            HStack(spacing: 5) {
                Image(systemName: locked ? "lock.fill" : "bolt.fill")
                    .font(.system(size: 12, weight: .semibold))
                Text("Agent")
                    .font(.caption.weight(.semibold))
            }
            .padding(.horizontal, 11)
            .padding(.vertical, 6)
            .foregroundStyle(isOn && !locked ? Color.white : Color.secondary)
            .glassSurface(in: RoundedRectangle(cornerRadius: 12), prominent: isOn && !locked)
            .overlay(
                RoundedRectangle(cornerRadius: 12)
                    .stroke(isOn && !locked ? Color.clear : Color.secondary.opacity(0.25), lineWidth: 0.5)
            )
        }
        .buttonStyle(.plain)
    }
}
```

（保留文件末尾的 `#Preview { ... }` 不动。）

- [ ] **Step 2: SparkView 加 Pro 提示状态**

在 `SparkView` 结构体的 `@State` 声明区（与其他 `@State` 放一起）加：

```swift
    @State private var showProUpsell = false
```

- [ ] **Step 3: SparkView 门控 Agent 芯片 + 隐藏 BYOK 模型芯片 + 会话上限横幅**

把这段（`SparkView.swift:157` 起）：

```swift
            HStack(spacing: 8) {
                SparkAgentChip(isOn: $viewModel.isAgentModeEnabled)

                if !viewModel.availableModels.isEmpty {
                    SparkModelChip(
                        title: viewModel.effectiveModelName,
                        icon: "cpu",
                        options: viewModel.availableModels.map { (id: $0, label: $0) },
                        selectedID: viewModel.effectiveModelName,
                        onSelect: { viewModel.selectModel($0) }
                    )
                }
```

替换为：

```swift
            if viewModel.sessionLimitReached {
                HStack(spacing: 12) {
                    Text("本次对话已达上限（\(SparkTierLimits.maxSessionRounds ?? 0) 轮）")
                        .font(.caption)
                        .foregroundStyle(.secondary)
                    Spacer()
                    Button("开启新会话") { viewModel.newConversation() }
                        .font(.caption.weight(.semibold))
                    Button("升级 Pro") { showProUpsell = true }
                        .font(.caption.weight(.semibold))
                }
                .padding(.horizontal, 20)
                .padding(.vertical, 6)
            }

            HStack(spacing: 8) {
                SparkAgentChip(
                    isOn: $viewModel.isAgentModeEnabled,
                    locked: !SparkTierLimits.isAgentAllowed,
                    onLockedTap: { showProUpsell = true }
                )
                .alert("升级 Pro 会员", isPresented: $showProUpsell) {
                    Button("知道了", role: .cancel) {}
                    // TODO(Phase 4): 改为 present PaywallView
                } message: {
                    Text("升级 Pro 即可解锁 Agent 智能体、更强模型与更长对话。")
                }

                #if NOTIEE_PLUS
                if !viewModel.availableModels.isEmpty {
                    SparkModelChip(
                        title: viewModel.effectiveModelName,
                        icon: "cpu",
                        options: viewModel.availableModels.map { (id: $0, label: $0) },
                        selectedID: viewModel.effectiveModelName,
                        onSelect: { viewModel.selectModel($0) }
                    )
                }
                #endif
```

> 说明：免费版 Spark 的模型由 `CuratedModelSelection`（锁 flash）决定，BYOK 的 `SparkModelChip` 在免费版无意义，故 `#if NOTIEE_PLUS` 隐藏。Agent 芯片的 `locked` 由 `SparkTierLimits.isAgentAllowed` 决定：Notiee+ 恒 true（不锁），免费档恒锁。

- [ ] **Step 4: AgentSettingsView 免费档加锁提示**

把 `AgentSettingsView` body 里第一个 `Section { Text("Agent 模式可在 Spark ...") ... }` **之前**插入：

```swift
            if !SparkTierLimits.isAgentAllowed {
                Section {
                    Label("Agent 模式为 Pro 会员专属", systemImage: "lock.fill")
                        .foregroundStyle(.secondary)
                        .font(.footnote)
                }
            }
```

- [ ] **Step 5: 编译两个 target 都通过（关键）**

Xcode：
1. `Notiee` scheme → `⌘B` → BUILD SUCCEEDED（Agent 芯片带锁、无 BYOK 模型芯片、AgentSettings 顶部有锁提示）。
2. `Notiee+` scheme → `⌘B` → BUILD SUCCEEDED（`SparkTierLimits.isAgentAllowed` 恒 true → 芯片不锁；`#if NOTIEE_PLUS` 内的模型芯片照旧；AgentSettings 无锁提示；会话上限横幅因 `sessionLimitReached` 恒 false 而不显示）。

- [ ] **Step 6: 手动核对**

- `Notiee`（免费）：Spark 底部 Agent 芯片带锁图标，点击弹「升级 Pro 会员」；没有模型芯片。连续对话到 20 轮后再发→出现「本次对话已达上限（20 轮）」横幅，「开启新会话」清空、「升级 Pro」弹窗。
- `Notiee+`：Agent 芯片可正常开关；模型芯片照旧；无横幅。

- [ ] **Step 7: 提交**

```bash
git add Notiee/Features/Spark/SparkAgentChip.swift \
        Notiee/Features/Spark/SparkView.swift \
        Notiee/Features/Settings/AgentSettingsView.swift
git commit -m "feat(spark): gate Agent behind Pro, hide BYOK model chip in free, session-limit banner"
```

---

# Part B — Phase 3：额度展示

## Task 4: EntitlementStore（只读额度展示态，TDD）

**Files:**
- Create: `Notiee/Services/EntitlementStore.swift`（只勾 Notiee target）
- Test: `NotieeTests/EntitlementStoreTests.swift`（只勾 Notiee 测试）

- [ ] **Step 1: 写失败的测试**

创建 `NotieeTests/EntitlementStoreTests.swift`：

```swift
import XCTest
@testable import Notiee

@MainActor
final class EntitlementStoreTests: XCTestCase {

    func testStartsWithNoQuotaAndFreeTier() {
        let store = EntitlementStore()
        XCTAssertNil(store.quota)
        XCTAssertEqual(store.tier, .free)
    }

    func testQuotaUpdatesFromBroadcast() {
        let store = EntitlementStore()
        let q = BackendQuota(tier: "pro", used: 12, limit: 300, remaining: 288)
        q.broadcast()

        let exp = expectation(description: "quota updated on main")
        DispatchQueue.main.async {
            XCTAssertEqual(store.quota, q)
            XCTAssertEqual(store.tier, .pro)
            exp.fulfill()
        }
        wait(for: [exp], timeout: 1.0)
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

`⌘U`。Expected: 编译失败 —— `EntitlementStore` 未定义。

- [ ] **Step 3: 写最小实现**

创建 `Notiee/Services/EntitlementStore.swift`（**只勾 Notiee target**）：

```swift
import Foundation
import Combine

/// 免费版的额度/档位**展示态**（只读，真值全在后端）。拉 `/me/quota`，并监听
/// `/ai/process` 捎带 quota 的 `.backendQuotaUpdated` 广播，免二次请求。
/// 仅用于 UI 展示，不参与任何 gating（gating 走 `CurrentEntitlement.tier`）。
/// 仅 Notiee（免费）target。Phase 4 由 StoreKit 购买流程扩展写入。
@MainActor
final class EntitlementStore: ObservableObject {
    static let shared = EntitlementStore()

    @Published private(set) var quota: BackendQuota?
    @Published private(set) var isLoading = false
    @Published private(set) var loadError: String?

    private let api: BackendAPIClient
    private var observer: NSObjectProtocol?

    init(api: BackendAPIClient = .shared) {
        self.api = api
        observer = NotificationCenter.default.addObserver(
            forName: .backendQuotaUpdated, object: nil, queue: .main
        ) { [weak self] note in
            guard let q = note.userInfo?["quota"] as? BackendQuota else { return }
            MainActor.assumeIsolated { self?.quota = q }
        }
    }

    deinit {
        if let observer { NotificationCenter.default.removeObserver(observer) }
    }

    /// 展示用档位。未知时按最保守的免费档处理。
    var tier: ModelTier { quota?.isPro == true ? .pro : .free }

    var isLoggedIn: Bool { AuthService.shared.bearerToken != nil }

    func refresh() async {
        guard isLoggedIn else { quota = nil; return }
        isLoading = true
        loadError = nil
        defer { isLoading = false }
        do {
            let json = try await api.getJSON(path: "me/quota")
            if let q = BackendQuota(dictionary: json) { quota = q }
        } catch let e as BackendError {
            loadError = e.message
        } catch {
            loadError = error.localizedDescription
        }
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

`⌘U`。Expected: `EntitlementStoreTests` PASS。

> 若 `MainActor.assumeIsolated` 在目标 iOS 版本不可用，改用 `Task { @MainActor in self?.quota = q }`（因 queue 已是 `.main`，二者等价）。

- [ ] **Step 5: 提交**

```bash
git add Notiee/Services/EntitlementStore.swift NotieeTests/EntitlementStoreTests.swift
git commit -m "feat(quota): read-only EntitlementStore backed by /me/quota + broadcast"
```

---

## Task 5: QuotaCard（额度环 + 篇数 + Pro 入口 + 状态兜底）

**Files:**
- Create: `Notiee/Features/Settings/QuotaCard.swift`（只勾 Notiee target）

> SwiftUI 视图，靠编译 + 手动核对；数据逻辑已在 Task 4 覆盖。

- [ ] **Step 1: 写视图**

创建 `Notiee/Features/Settings/QuotaCard.swift`（**只勾 Notiee target**）：

```swift
import SwiftUI

/// 「我」tab 的额度卡片：进度环 + 「本月 X / N 篇」+ 档位 + Pro 入口。
/// 断网/未登录/加载中都有兜底态。真值来自 `EntitlementStore`。仅 Notiee target。
struct QuotaCard: View {
    @StateObject private var entitlement = EntitlementStore.shared
    @State private var showProUpsell = false

    var body: some View {
        VStack(alignment: .leading, spacing: 12) {
            if !entitlement.isLoggedIn {
                Label("登录后查看本月额度", systemImage: "person.crop.circle.badge.questionmark")
                    .font(.subheadline)
                    .foregroundStyle(.secondary)
            } else if let q = entitlement.quota {
                content(q)
            } else if entitlement.isLoading {
                HStack { ProgressView(); Text("加载额度…").font(.subheadline).foregroundStyle(.secondary) }
            } else {
                HStack {
                    Text(entitlement.loadError == nil ? "暂无额度信息" : "额度加载失败")
                        .font(.subheadline).foregroundStyle(.secondary)
                    Spacer()
                    Button("重试") { Task { await entitlement.refresh() } }
                        .font(.caption.weight(.semibold))
                }
            }
        }
        .padding(.vertical, 4)
        .task { await entitlement.refresh() }
        .alert("升级 Pro 会员", isPresented: $showProUpsell) {
            Button("知道了", role: .cancel) {}
            // TODO(Phase 4): present PaywallView
        } message: {
            Text("升级 Pro 即可大幅提升每月额度，并解锁全部模型。")
        }
    }

    @ViewBuilder
    private func content(_ q: BackendQuota) -> some View {
        HStack(spacing: 16) {
            ring(used: q.used, limit: q.limit)
                .frame(width: 56, height: 56)

            VStack(alignment: .leading, spacing: 4) {
                Text(q.isPro ? "Pro 会员" : "免费版")
                    .font(.subheadline.weight(.semibold))
                Text("本月 \(q.used) / \(q.limit) 篇")
                    .font(.headline)
                Text("剩余 \(max(0, q.remaining)) 篇")
                    .font(.caption)
                    .foregroundStyle(.secondary)
            }

            Spacer()

            if !q.isPro {
                Button {
                    showProUpsell = true
                } label: {
                    Text("升级 Pro")
                        .font(.caption.weight(.semibold))
                        .padding(.horizontal, 12).padding(.vertical, 6)
                        .background(Capsule().fill(Color.accentColor.opacity(0.15)))
                }
                .buttonStyle(.plain)
            }
        }
    }

    private func ring(used: Int, limit: Int) -> some View {
        let fraction = limit > 0 ? min(1, Double(used) / Double(limit)) : 0
        return ZStack {
            Circle().stroke(Color.secondary.opacity(0.15), lineWidth: 6)
            Circle()
                .trim(from: 0, to: fraction)
                .stroke(Color.accentColor, style: StrokeStyle(lineWidth: 6, lineCap: .round))
                .rotationEffect(.degrees(-90))
            Text("\(Int(fraction * 100))%")
                .font(.caption2.weight(.semibold))
        }
    }
}
```

- [ ] **Step 2: 编译确认通过**

`Notiee` scheme → `⌘B`。Expected: BUILD SUCCEEDED。

- [ ] **Step 3: 提交**

```bash
git add Notiee/Features/Settings/QuotaCard.swift
git commit -m "feat(quota): QuotaCard with progress ring, X/N 篇, Pro upsell and fallback states"
```

---

## Task 6: UsageReviewView（篇数版回顾）

**Files:**
- Create: `Notiee/Features/Settings/UsageReviewView.swift`（只勾 Notiee target）

- [ ] **Step 1: 写视图**

创建 `Notiee/Features/Settings/UsageReviewView.swift`（**只勾 Notiee target**）：

```swift
import SwiftUI

/// 免费版「回顾」：以「篇数」为口径（替代 Notiee+ 的 token 版 `ReviewView`）。
/// 顶部 QuotaCard 展示本月额度，下面按笔记标题聚合本月拍记篇数。仅 Notiee target。
struct UsageReviewView: View {
    @ObservedObject var store: NotieeStore

    private var thisMonthRecords: [NoteRecord] {
        let cal = Calendar.current
        return store.records.filter {
            !$0.isDeleted && cal.isDate($0.capturedAt, equalTo: Date(), toGranularity: .month)
        }
    }

    /// 按标题聚合的本月篇数（Top 8），降序。
    private var topGroups: [(title: String, count: Int)] {
        Dictionary(grouping: thisMonthRecords, by: { $0.title })
            .map { (title: $0.key.isEmpty ? "未命名" : $0.key, count: $0.value.count) }
            .sorted { $0.count > $1.count }
            .prefix(8)
            .map { $0 }
    }

    var body: some View {
        List {
            Section {
                QuotaCard()
            }

            Section("本月拍记") {
                HStack {
                    Text("共处理")
                    Spacer()
                    Text("\(thisMonthRecords.count) 篇")
                        .font(.headline)
                }
            }

            if !topGroups.isEmpty {
                Section("按笔记分布") {
                    ForEach(topGroups, id: \.title) { group in
                        HStack {
                            Text(group.title).lineLimit(1)
                            Spacer()
                            Text("\(group.count) 篇")
                                .font(.subheadline.weight(.semibold))
                                .foregroundStyle(.secondary)
                        }
                    }
                }
            }
        }
        .navigationTitle("回顾")
        .navigationBarTitleDisplayMode(.inline)
    }
}
```

- [ ] **Step 2: 编译确认通过**

`Notiee` scheme → `⌘B`。Expected: BUILD SUCCEEDED。

- [ ] **Step 3: 提交**

```bash
git add Notiee/Features/Settings/UsageReviewView.swift
git commit -m "feat(quota): UsageReviewView (篇数-based review) for free version"
```

---

## Task 7: MeView 分叉（插 QuotaCard + 切回顾页）

**Files:**
- Modify: `Notiee/Features/Settings/MeView.swift`（账户 Section 后插 QuotaCard；回顾入口 `#if` 分叉）

- [ ] **Step 1: 账户 Section 之后插入 QuotaCard（仅免费版）**

在 `MeView` 里，找到账户 Section 结束、下一个 `Section { NavigationLink { AllSchedulesView(store: store) ...` 之前，插入：

```swift
            #if !NOTIEE_PLUS
            Section {
                QuotaCard()
            }
            #endif
```

即插在这一行之前：

```swift
            Section {
                NavigationLink {
                    AllSchedulesView(store: store)
                } label: {
                    Label("全部日程", systemImage: "calendar")
                        .foregroundColor(NotieeColors.themed(.orange))
                }
```

- [ ] **Step 2: 回顾入口按 target 分叉**

把回顾入口：

```swift
            Section {
                NavigationLink {
                    ReviewView(store: store)
                } label: {
                    Label("回顾", systemImage: "chart.pie.fill")
                        .foregroundColor(NotieeColors.themed(.blue))
                }
            }
```

替换为：

```swift
            Section {
                NavigationLink {
                    #if NOTIEE_PLUS
                    ReviewView(store: store)
                    #else
                    UsageReviewView(store: store)
                    #endif
                } label: {
                    Label("回顾", systemImage: "chart.pie.fill")
                        .foregroundColor(NotieeColors.themed(.blue))
                }
            }
```

- [ ] **Step 3: 编译两个 target 都通过（关键）**

1. `Notiee` scheme → `⌘B` → BUILD SUCCEEDED（账户下有额度卡片；回顾进 `UsageReviewView`）。
2. `Notiee+` scheme → `⌘B` → BUILD SUCCEEDED（**无** QuotaCard/EntitlementStore/UsageReviewView 参与编译；回顾仍进 token 版 `ReviewView`）。

- [ ] **Step 4: 手动核对**

- `Notiee`（免费）：「我」tab 账户下方出现额度卡片（登录后显示「本月 X/N 篇」+ 环 + 升级 Pro；未登录显示提示）；点「回顾」进篇数版页面。
- `Notiee+`：无额度卡片；点「回顾」进原 token 版 `ReviewView`，一切照旧。

- [ ] **Step 5: 提交**

```bash
git add Notiee/Features/Settings/MeView.swift
git commit -m "feat(quota): free version shows QuotaCard and 篇数-based review in Me tab"
```

---

## Self-Review（作者已核对）

**Spec 覆盖（对照原 §4 Phase 2.5 / Phase 3）：**
- Phase 2.5：`SparkTierLimits`（Task 1，中心出口 `#if NOTIEE_PLUS`）✅；单会话 20 轮（Task 2）✅；记忆 5 条（Task 2）✅；Agent 仅 Pro（Task 3，`SparkAgentChip` locked + `AgentSettingsView` 锁提示）✅；免费档锁 flash —— 已由 Phase 2 的 `CuratedModelSelection` 夹取实现，`SparkTierLimits.isTextModelAllowed` 提供中心判定，BYOK 模型芯片在免费版隐藏（Task 3）✅。
  - **修正**：`SparkModelPreferences.swift` 原清单标注要改，核查后**不需要**——免费版 Spark 走 `BackendSparkAIService` + `CuratedModelSelection`，不经 `SparkModelPreferences`（那是 BYOK/`SparkAIService` 路径）。故未纳入，避免无谓改动。
- Phase 3：`QuotaCard`（Task 5）✅；`MeView` 插卡（Task 7）✅；`UsageReviewView`（Task 6）✅；回顾入口 `#if` 分叉（Task 7）✅；断网/未登录/加载失败兜底态（Task 5 QuotaCard 三态）✅。
  - **新增**：`EntitlementStore`（Task 4）——Phase 3 展示所必需的额度真值载体（原文档把它记在 Phase 4，但那是「购买写入」侧；此处只建「后端读取展示」侧，Phase 4 再扩展）。

**占位符扫描：** 无 TBD/空实现。两处 `TODO(Phase 4): PaywallView` 是**有意的**跨阶段接点，非占位——当前用 alert 完整可用。

**类型一致性：** `SparkTierLimits.{maxSessionRounds,maxMemoryEntries,isAgentAllowed,isTextModelAllowed(_:),sessionRoundLimitReached(currentRounds:),acceptedNewMemoryCount(existingCount:incoming:)}` 定义（Task 1）与调用（Task 2/3）签名一致。`EntitlementStore.{quota,tier,isLoggedIn,isLoading,loadError,refresh()}` 定义（Task 4）与 `QuotaCard`（Task 5）使用一致。`BackendQuota(tier:used:limit:remaining:)` / `.isPro` / `broadcast()` / `.backendQuotaUpdated` 均沿用现有 Phase 0 定义。`SparkAgentChip(isOn:locked:onLockedTap:)` 新签名（Task 3 Step 1）与 `SparkView` 调用（Step 3）一致，默认参数保证 `#Preview` 及 Notiee+ 其它调用点不破。

**双 target 编译不变式：** 每个碰共享文件的 Task 都有「两个 target 都 `⌘B`」步骤（Task 3 Step 5、Task 7 Step 3）。Notiee-only 类型（`QuotaCard`/`EntitlementStore`/`UsageReviewView`）在 `MeView` 里全部包在 `#if !NOTIEE_PLUS` / `#if NOTIEE_PLUS #else` 内，Notiee+ 不参与编译。

**已知前提：** gating 用硬编码 `CurrentEntitlement.tier == .free`，故 `SparkTierLimits` 免费分支恒生效、`EntitlementStore.tier` 仅供展示；Phase 4 把 `CurrentEntitlement.tier` 接到真档位后，本 Phase 的限额与展示逻辑均无需改动即自动对 Pro 用户放开。
